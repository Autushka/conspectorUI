viewControllers.controller('deficienciesView', function($scope, $rootScope, $filter, $q, ngTableParams, dataSrv, globalSrv) {
	$scope.refreshViewTE = function() {
	};
	
	$scope.mode = "display";

	$scope.refreshViewTE();
});